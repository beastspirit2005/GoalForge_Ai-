{
  "name": "goalforge-ai-root",
  "version": "1.0.0",
  "description": "Unified runner for GoalForge AI frontend and backend",
  "scripts": {
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "cd backend && python -m uvicorn app.main:app --host 0.0.0.0 --port 8001",
    "dev": "npx concurrently -n \"BACKEND,FRONTEND\" -c \"cyan,magenta\" \"npm run dev:backend\" \"npm run dev:frontend\"",
    "deploy:vercel": "powershell -ExecutionPolicy Bypass -File scripts/vercel-deploy.ps1"
  }
}
